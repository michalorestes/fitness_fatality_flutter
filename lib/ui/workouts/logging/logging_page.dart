import 'package:flutter/material.dart';

class LoggingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
   
    return Scaffold(
      appBar: AppBar(
        title: Text("Logging"),
      ),
      body: Center(
        child: Text("Let's get in to training"),
      ),
    );
  }
}